// This file is intentionally left minimal.
// The original counter smoke test has been replaced because the app no longer
// has a counter widget.  Candidates should add their own widget tests.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // TODO: add widget tests
    expect(true, isTrue);
  });
}

